# The isolated maintenance link has no route or DNS, so use the workstation
# relay while it is connected. That relay is configured from Fermilab's
# canonical <redacted-site-host> pool. Direct Fermilab sources remain the fallback
# after the registered interface receives a route and DNS.
[Time]
NTP=<redacted-private-ip>
FallbackNTP=<redacted-site-host> <redacted-site-host> <redacted-site-host> <redacted-site-host> <redacted-site-host> <redacted-site-host>
RootDistanceMaxSec=15
PollIntervalMinSec=32
PollIntervalMaxSec=2048
